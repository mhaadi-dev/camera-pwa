export default function OfflinePage() {
    return (
      <div>
        <h1>Offline Mode</h1>
        <p>You are offline. You can still use the camera and view saved photos.</p>
      </div>
    );
  }